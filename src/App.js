import "./App.css";
import SortableList from "./components/SortableList";

const App = () => {
  return (
    <div className='container'>
      <SortableList />
    </div>
  );
};

export default App;
